{
  "name": "vaultmind",
  "version": "0.1.0",
  "description": "VaultMind — chat with your Obsidian vault (RAG)",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.21.5",
    "obsidian": "latest",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "minisearch": "^7.1.0"
  }
}
